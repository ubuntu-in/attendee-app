# Copy to `.env` for local overrides only. No values go in this file.
# Public event configuration lives in src/config.ts.
# Secrets (Firebase Web Push, Cloudflare Worker) belong in their platform stores, never Git.